#include <stdio.h>

/**
 * main - print single digit numbers
 *
 * Description: print single digit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	while (i = 48, i < 58, i++)
	{
		putchar(10);
	}
	putchar(10);
	return (0);
}
